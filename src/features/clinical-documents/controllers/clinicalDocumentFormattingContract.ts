/**
 * Shared formatting contract for the rich-text clinical document editor.
 * Centralizes values that must stay aligned across DOM editing, sanitization,
 * and plain-text/PDF export.
 */
export const CLINICAL_DOCUMENT_INDENT_STEP_PX = 24;
