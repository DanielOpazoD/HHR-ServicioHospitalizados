/**
 * @deprecated Prefer importing context contracts directly from
 * `@/context/dailyRecordContextContracts`.
 *
 * This shim remains only for backward compatibility while legacy imports are
 * drained from the hooks surface.
 */
export type * from '@/context/dailyRecordContextContracts';
