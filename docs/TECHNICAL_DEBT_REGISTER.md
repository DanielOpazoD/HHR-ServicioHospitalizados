# Technical Debt Register

| Estado  | Owner             | Area               | Item                                                                                                         | Criterio de cierre                                                                                    |
| ------- | ----------------- | ------------------ | ------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------- |
| cerrado | architecture      | quality-green      | Recuperar `check:quality` sin relajar guardrails, corrigiendo gobernanza, boundaries y ciclos activos        | `check:quality` y `ci:pre-merge` verdes al cierre del ciclo `R00-R06`                                 |
| cerrado | frontend-platform | local-pin-ux       | Rebajar el PIN local a barrera UX explícita, sin presentarlo como seguridad fuerte                           | Cerrado en ciclo `Q00-Q07`; tracker y documentación lo mantienen como barrera UX                      |
| cerrado | architecture      | feature-public-api | Consolidar entrypoints públicos mínimos para `census`, `clinical-documents` y `laboratory`                   | Cerrado en ciclo `Q00-Q07`; boundaries y `quality-metrics` siguen en `0`                              |
| cerrado | qa                | critical-coverage  | Sostener `src/features/clinical-documents` sobre baseline crítica tras recuperar la zona con tests dirigidos | `check:critical-coverage` y `reports/critical-coverage.json` siguen en `passing` al cierre de `R06`   |
| cerrado | architecture      | boundary-drift     | Bajar el residual de convergencia reportado en `quality-metrics` sin abrir capas nuevas                      | `featureBoundaryViolations=0` y `dailyRecordBoundaryViolations=0` al cierre de `R06`                  |
| cerrado | qa                | flow-performance   | Mantener `reports/e2e/flow-performance-budget.*` generables desde un puerto aislado y sin reuse accidental   | `test:e2e:flow-performance`, `check:flow-performance-budget` y `operational-health` en `passing`      |
| cerrado | docs              | report-drift       | Mantener alineados README, trackers y artefactos canónicos como `technical-execution-baseline`               | `check:docs-drift` verde y reportes canónicos regenerados al cierre de `R06`                          |
| cerrado | frontend-platform | hotspots           | Reducir deuda focalizada en `patient-row` / `indexedDbCore`; diferir `BaseModal` si no bloquea el cierre     | Dos hotspots intervenidos con tests y guardrails verdes; `BaseModal` queda como mantenimiento normal  |
| cerrado | frontend-platform | census-type-drift  | Alinear tests de `census` al modelo `unifiedRows` y retirar expectativas sobre `occupiedRows` / `emptyBeds`  | `typecheck` vuelve a verde y los tests de `census` solo consumen la API vigente                       |
| activo  | frontend-platform | foundations        | Mantener la convergencia lograda por bloques (`feature public APIs`, shims, megatests, hygiene)              | La cadencia mensual corre sin reapertura de deep imports, shims nuevos ni drift de ownership          |
| cerrado | frontend-platform | firebase-runtime   | Reducir `console.warn/error` legacy fuera de sinks estructurados                                             | `reports/quality-metrics.md` reporta `0` usos raw fuera de `loggerConsoleSink`                        |
| activo  | architecture      | permissions        | Mantener `operationalAccessPolicy` como unica fachada permitida sobre `utils/permissions`                    | Solo `shared/access/operationalAccessPolicy.ts` importa el legacy                                     |
| activo  | platform          | functions-security | Mantener explícita la aceptación temporal de vulnerabilidades `low` upstream en `functions`                  | Existe registro documentado, reevaluación al cambiar `firebase-admin/functions` y sin `high/critical` |
| cerrado | architecture      | domain-hotspots    | Concentrar `patient.ts` y `dailyRecord.ts` en ports/facades y evitar consumers directos nuevos               | `check:domain-hotspot-boundary` deja solo ports y facades aprobadas                                   |
| cerrado | architecture      | legacy-firebase    | Simplificar bridges de imports sin tocar la compatibilidad de lectura y normalizacion legacy                 | La app sigue abriendo registros legacy desde Firebase por el boundary explicito                       |
| cerrado | platform          | firestore services | Extender inyección explícita a flows restantes de backup/export y auth con runtime singleton                 | Los servicios críticos usan `create...Service` o runtime inyectable compatible                        |
| cerrado | platform          | firestore services | Completar inyección explícita en auditoría, roles y export clínico                                           | Los servicios ya aceptan runtime inyectable y mantienen export por defecto                            |
| cerrado | qa                | critical coverage  | Formalizar `src/services/backup` como zona crítica con baseline instrumentada y ownership                    | `check:critical-coverage` incluye `src/services/backup` y el mapa técnico lo trata como gate crítico  |
| cerrado | qa                | critical coverage  | Ampliar zonas críticas hacia app shell, auth bootstrap y reminders                                           | `check:critical-coverage` incluye esas zonas con baseline estable                                     |
| cerrado | qa                | phase-1 confidence | Resolver regresiones activas de `NameInput` y `handleShareLink` y restablecer `test:ci:unit`                 | `test:ci:unit` vuelve a verde sin fallos abiertos ni cuarentenas                                      |
