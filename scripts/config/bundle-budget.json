{
  "entryMaxBytes": 700000,
  "chunkMaxBytes": 1250000,
  "assetPatternBudgets": [
    {
      "pattern": "^vendor/exceljs\\.min\\.js$",
      "_comment": "Browser ExcelJS is served as a runtime asset instead of a duplicated Vite chunk. This ceiling keeps roughly 15% headroom over the measured ~925 KB asset so routine lockfile rebuilds do not create noisy near-limit release warnings.",
      "maxBytes": 1100000
    }
  ],
  "startupChunkBudgets": [
    {
      "label": "entry-app",
      "source": "entry",
      "maxBytes": 120000,
      "severity": "error"
    },
    {
      "label": "firebase-core",
      "pattern": "^vendor-firebase-core-.*\\.js$",
      "_comment": "Raised from 320000 to 350000. The chunk sat at 91% of the older ceiling with Firebase SDK 12; minor Firebase patch releases were landing on the edge of the budget. Upstream-controlled size, so give explicit headroom. Revisit if utilization exceeds 93%.",
      "maxBytes": 350000,
      "severity": "error"
    },
    {
      "label": "app-authenticated-shell",
      "pattern": "^app-authenticated-shell-.*\\.js$",
      "_comment": "Raised from 560000 to 620000 after measuring the authenticated shell at ~534 KB with ES2022 + two-pass production minification. Keep direct-import guardrails in src/tests/build/chunkingPolicy.test.ts and revisit if utilization exceeds 90%.",
      "maxBytes": 620000,
      "severity": "error"
    }
  ],
  "chunkPatternBudgets": [
    {
      "pattern": "^vendor-firebase-core-.*\\.js$",
      "_comment": "Kept in sync with the firebase-core startup budget above.",
      "maxBytes": 350000
    },
    {
      "pattern": "^vendor-firebase-firestore-.*\\.js$",
      "maxBytes": 500000
    },
    {
      "pattern": "^vendor-firebase-aux-.*\\.js$",
      "maxBytes": 120000
    },
    {
      "pattern": "^vendor-excel-(?!core-).*\\.js$",
      "maxBytes": 950000
    },
    {
      "pattern": "^pdf-.*\\.js$",
      "_comment": "PDF.js worker/runtime remains async, but this protects the large PDF viewer chunk from silently drifting toward the global chunk limit.",
      "maxBytes": 560000
    },
    {
      "pattern": "^TransferManagementView-.*\\.js$",
      "maxBytes": 750000
    }
  ]
}
